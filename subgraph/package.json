{
  "name": "",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen subgraph.yaml",
    "build": "graph build subgraph.yaml",
    "deploy:studio": "graph deploy --node https://api.studio.thegraph.com/deploy/ ",
    "deploy:hosted": "graph deploy --product hosted-service --node https://api.studio.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ ",
    "create-local": "graph create --node http://localhost:8020/ ",
    "remove-local": "graph remove --node http://localhost:8020/ ",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 "
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.2",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
