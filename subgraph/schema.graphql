type Tweet @entity {
  id: ID!
  userAddress: Bytes! # address
  tokenId: BigInt! # uint256
  title: String! # string
  body: String! # string
  image: String! # string
  createAt: BigInt! # uint256
  like: Int!
  #  user: User!
}

type Like @entity {
  id: ID!
  userAddress: Bytes! # address
  tokenId: BigInt! # uint256
}

# TODO
type User @entity {
  id: ID!
  userId: BigInt!
  address: String!
  name: String!
  image: String!
}
